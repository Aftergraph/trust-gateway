{
  "name": "agent-workforce",
  "version": "0.1.0",
  "private": true,
  "description": "A governed AI workforce: specialist bots with persistent memory, isolated tools, model routing — behind one Trust Gateway (fail-closed policy + tamper-evident audit).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "demo": "node demo/live-demo.js",
    "start": "node src/gateway/server.js"
  },
  "engines": { "node": ">=20" }
}